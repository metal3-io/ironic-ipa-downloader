name: build-images-action-pr

on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]

permissions:
  contents: read

jobs:
  build_ipa_downloader:
    name: Build ironic-ipa-downloader container image
    if: github.repository == 'metal3-io/ironic-ipa-downloader'
    permissions:
      contents: read
    uses: metal3-io/project-infra/.github/workflows/container-image-build.yml@main # zizmor: ignore[unpinned-uses]
    with:
      image-name: 'ironic-ipa-downloader'
      pushImage: false
      generate-sbom: false
      sign-image: false

